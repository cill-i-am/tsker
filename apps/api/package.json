{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "description": "Adaptive requirements API built with Effect Platform on Node.js",
  "type": "module",
  "scripts": {
    "dev": "portless api.tsker tsx watch --conditions=development ./bin/dev.ts",
    "dev:e2e": "tsx --conditions=development ./bin/dev.ts",
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "start": "node ./bin/start.mjs",
    "test": "vitest run",
    "lint": "ultracite check src",
    "type-check": "tsc --noEmit",
    "check-types": "pnpm run type-check"
  },
  "dependencies": {
    "@effect/platform": "0.94.5",
    "@effect/platform-node": "0.104.1",
    "effect": "3.19.18"
  },
  "devDependencies": {
    "@effect/language-service": "^0.73.1",
    "@types/node": "^22.15.3",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.20.6",
    "typescript": "5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
