# API App

Effect-based API service with health endpoints and observability.

## Commands

- `pnpm turbo run dev --filter=api`
- `pnpm turbo run test --filter=api`
- `pnpm turbo run build --filter=api`

## Health

- `GET /health/live`
- `GET /health/ready`

## Environment

See `.env.example`.
